node_modules/
dist/
.wrangler/
.dev.vars
.DS_Store

# `npx wrangler types` 生成物 (CI の post_install_script で再生成される)
worker-configuration.d.ts

# Vitest coverage reports
coverage/

# ローカル開発では @ippoan/mcp-cf-workers を file:../mcp-cf-workers で link して
# typecheck / test する (CCoW は GitHub Packages の read token を持たない)。その
# file link を含む package-lock.json は CI を壊すので commit しない。CI は
# frontend-ci.yml が npm_scope:'@ippoan' で都度 npm install して dev tag を引く。
package-lock.json
